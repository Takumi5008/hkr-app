# タスクリスト — 回線別リードタイム（獲得〜開通）平均日数

## タスク

- [x] API: `/api/report` に集計クエリを追加し `leadTimeByType` / `leadTimeOverall` を返す
- [x] Page: `/team-report` に「回線別リードタイム」セクションを追加
- [x] `docs/functional-design.md` の API設計セクションに `/api/report` レスポンス追記
- [x] 型チェック（`tsc --noEmit` 相当）
- [x] 動作確認（`next build`）
- [x] コミット
